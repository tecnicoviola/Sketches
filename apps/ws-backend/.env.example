# WebSocket Backend Environment Variables

# Port for WebSocket Backend server
PORT=8080

# Secret key for JWT token verification (must match http-backend)
JWT_SECRET=your_jwt_secret_here

# PostgreSQL Database connection string (if persistence is enabled)
DATABASE_URL=postgresql://user:password@localhost:5432/sketches
